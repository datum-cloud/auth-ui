apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: { name: auth-ui, labels: { app.kubernetes.io/name: auth-ui } }
spec:
  selector: { matchLabels: { app.kubernetes.io/name: auth-ui } }
  endpoints: [{ port: http, path: /metrics }]
